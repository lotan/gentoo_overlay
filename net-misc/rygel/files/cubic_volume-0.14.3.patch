diff -Naur rygel-0.14.3.old/Makefile.in rygel-0.14.3/Makefile.in
--- rygel-0.14.3.old/Makefile.in	2012-08-07 21:48:51.000000000 +0200
+++ rygel-0.14.3/Makefile.in	2012-10-14 20:34:06.457559194 +0200
@@ -16,6 +16,23 @@
 @SET_MAKE@
 
 VPATH = @srcdir@
+am__make_dryrun = \
+  { \
+    am__dry=no; \
+    case $$MAKEFLAGS in \
+      *\\[\ \	]*) \
+        echo 'am--echo: ; @echo "AM"  OK' | $(MAKE) -f - 2>/dev/null \
+          | grep '^AM OK$$' >/dev/null || am__dry=yes;; \
+      *) \
+        for am__flg in $$MAKEFLAGS; do \
+          case $$am__flg in \
+            *=*|--*) ;; \
+            *n*) am__dry=yes; break;; \
+          esac; \
+        done;; \
+    esac; \
+    test $$am__dry = yes; \
+  }
 pkgdatadir = $(datadir)/@PACKAGE@
 pkgincludedir = $(includedir)/@PACKAGE@
 pkglibdir = $(libdir)/@PACKAGE@
@@ -70,6 +87,11 @@
 	install-pdf-recursive install-ps-recursive install-recursive \
 	installcheck-recursive installdirs-recursive pdf-recursive \
 	ps-recursive uninstall-recursive
+am__can_run_installinfo = \
+  case $$AM_UPDATE_INFO_DIR in \
+    n|no|NO) false;; \
+    *) (install-info --version) >/dev/null 2>&1;; \
+  esac
 am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
 am__vpath_adj = case $$p in \
     $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
@@ -193,6 +215,8 @@
 GSTREAMER_APP_LIBS = @GSTREAMER_APP_LIBS@
 GSTREAMER_BASE_CFLAGS = @GSTREAMER_BASE_CFLAGS@
 GSTREAMER_BASE_LIBS = @GSTREAMER_BASE_LIBS@
+GSTREAMER_INTERFACES_CFLAGS = @GSTREAMER_INTERFACES_CFLAGS@
+GSTREAMER_INTERFACES_LIBS = @GSTREAMER_INTERFACES_LIBS@
 GSTREAMER_TAG_CFLAGS = @GSTREAMER_TAG_CFLAGS@
 GSTREAMER_TAG_LIBS = @GSTREAMER_TAG_LIBS@
 GST_PBU_CFLAGS = @GST_PBU_CFLAGS@
@@ -419,8 +443,11 @@
 	-rm -f libtool config.lt
 install-pkgconfigDATA: $(pkgconfig_DATA)
 	@$(NORMAL_INSTALL)
-	test -z "$(pkgconfigdir)" || $(MKDIR_P) "$(DESTDIR)$(pkgconfigdir)"
 	@list='$(pkgconfig_DATA)'; test -n "$(pkgconfigdir)" || list=; \
+	if test -n "$$list"; then \
+	  echo " $(MKDIR_P) '$(DESTDIR)$(pkgconfigdir)'"; \
+	  $(MKDIR_P) "$(DESTDIR)$(pkgconfigdir)" || exit 1; \
+	fi; \
 	for p in $$list; do \
 	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
 	  echo "$$d$$p"; \
@@ -605,13 +632,10 @@
 	done
 	@list='$(DIST_SUBDIRS)'; for subdir in $$list; do \
 	  if test "$$subdir" = .; then :; else \
-	    test -d "$(distdir)/$$subdir" \
-	    || $(MKDIR_P) "$(distdir)/$$subdir" \
-	    || exit 1; \
-	  fi; \
-	done
-	@list='$(DIST_SUBDIRS)'; for subdir in $$list; do \
-	  if test "$$subdir" = .; then :; else \
+	    $(am__make_dryrun) \
+	      || test -d "$(distdir)/$$subdir" \
+	      || $(MKDIR_P) "$(distdir)/$$subdir" \
+	      || exit 1; \
 	    dir1=$$subdir; dir2="$(distdir)/$$subdir"; \
 	    $(am__relativize); \
 	    new_distdir=$$reldir; \
@@ -699,7 +723,7 @@
 	*.zip*) \
 	  unzip $(distdir).zip ;;\
 	esac
-	chmod -R a-w $(distdir); chmod a+w $(distdir)
+	chmod -R a-w $(distdir); chmod u+w $(distdir)
 	mkdir $(distdir)/_build
 	mkdir $(distdir)/_inst
 	chmod a-w $(distdir)
diff -Naur rygel-0.14.3.old/common.am rygel-0.14.3/common.am
--- rygel-0.14.3.old/common.am	2012-08-07 21:39:00.000000000 +0200
+++ rygel-0.14.3/common.am	2012-10-14 20:38:28.154744289 +0200
@@ -24,6 +24,7 @@
 	$(LIBGUPNP_AV_CFLAGS) \
 	$(LIBGSTREAMER_CFLAGS) \
 	$(GSTREAMER_BASE_CFLAGS) \
+	$(GSTREAMER_INTERFACES_CFLAGS) \
 	$(GEE_CFLAGS) \
 	$(UUID_CFLAGS) \
 	$(COVERAGE_CFLAGS) \
@@ -37,6 +38,7 @@
 	$(LIBGUPNP_AV_LIBS) \
 	$(LIBGSTREAMER_LIBS) \
 	$(GSTREAMER_BASE_LIBS) \
+	$(GSTREAMER_INTERFACES_LIBS) \
 	$(GEE_LIBS) \
 	$(UUID_LIBS) \
 	$(COVERAGE_LIBS)
@@ -49,7 +51,7 @@
 RYGEL_COMMON_VALAFLAGS = \
 	--pkg rygel-build-config --pkg gupnp-1.0 \
 	--vapidir=$(rygeldir) \
-	--pkg gupnp-av-1.0 --pkg gee-1.0 \
+	--pkg gupnp-av-1.0 --pkg gee-1.0 --pkg gstreamer-interfaces-0.10 \
 	$(RYGEL_DEBUG_VALAFLAGS) \
 	$(COVERAGE_VALAFLAGS)
 
diff -Naur rygel-0.14.3.old/configure rygel-0.14.3/configure
--- rygel-0.14.3.old/configure	2012-08-07 21:48:48.000000000 +0200
+++ rygel-0.14.3/configure	2012-10-14 20:34:03.924576760 +0200
@@ -789,6 +789,8 @@
 LIBGIO_CFLAGS
 GST_PBU_LIBS
 GST_PBU_CFLAGS
+GSTREAMER_INTERFACES_LIBS
+GSTREAMER_INTERFACES_CFLAGS
 GSTREAMER_BASE_LIBS
 GSTREAMER_BASE_CFLAGS
 LIBGSTREAMER_LIBS
@@ -971,6 +973,8 @@
 LIBGSTREAMER_LIBS
 GSTREAMER_BASE_CFLAGS
 GSTREAMER_BASE_LIBS
+GSTREAMER_INTERFACES_CFLAGS
+GSTREAMER_INTERFACES_LIBS
 GST_PBU_CFLAGS
 GST_PBU_LIBS
 LIBGIO_CFLAGS
@@ -1691,6 +1695,10 @@
               C compiler flags for GSTREAMER_BASE, overriding pkg-config
   GSTREAMER_BASE_LIBS
               linker flags for GSTREAMER_BASE, overriding pkg-config
+  GSTREAMER_INTERFACES_CFLAGS
+              C compiler flags for GSTREAMER_INTERFACES, overriding pkg-config
+  GSTREAMER_INTERFACES_LIBS
+              linker flags for GSTREAMER_INTERFACES, overriding pkg-config
   GST_PBU_CFLAGS
               C compiler flags for GST_PBU, overriding pkg-config
   GST_PBU_LIBS
@@ -13196,6 +13204,97 @@
 fi
 
 pkg_failed=no
+{ $as_echo "$as_me:${as_lineno-$LINENO}: checking for GSTREAMER_INTERFACES" >&5
+$as_echo_n "checking for GSTREAMER_INTERFACES... " >&6; }
+
+if test -n "$GSTREAMER_INTERFACES_CFLAGS"; then
+    pkg_cv_GSTREAMER_INTERFACES_CFLAGS="$GSTREAMER_INTERFACES_CFLAGS"
+ elif test -n "$PKG_CONFIG"; then
+    if test -n "$PKG_CONFIG" && \
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"gstreamer-interfaces-0.10 >= \$GSTREAMER_REQUIRED\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "gstreamer-interfaces-0.10 >= $GSTREAMER_REQUIRED") 2>&5
+  ac_status=$?
+  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
+  test $ac_status = 0; }; then
+  pkg_cv_GSTREAMER_INTERFACES_CFLAGS=`$PKG_CONFIG --cflags "gstreamer-interfaces-0.10 >= $GSTREAMER_REQUIRED" 2>/dev/null`
+		      test "x$?" != "x0" && pkg_failed=yes
+else
+  pkg_failed=yes
+fi
+ else
+    pkg_failed=untried
+fi
+if test -n "$GSTREAMER_INTERFACES_LIBS"; then
+    pkg_cv_GSTREAMER_INTERFACES_LIBS="$GSTREAMER_INTERFACES_LIBS"
+ elif test -n "$PKG_CONFIG"; then
+    if test -n "$PKG_CONFIG" && \
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"gstreamer-interfaces-0.10 >= \$GSTREAMER_REQUIRED\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "gstreamer-interfaces-0.10 >= $GSTREAMER_REQUIRED") 2>&5
+  ac_status=$?
+  $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
+  test $ac_status = 0; }; then
+  pkg_cv_GSTREAMER_INTERFACES_LIBS=`$PKG_CONFIG --libs "gstreamer-interfaces-0.10 >= $GSTREAMER_REQUIRED" 2>/dev/null`
+		      test "x$?" != "x0" && pkg_failed=yes
+else
+  pkg_failed=yes
+fi
+ else
+    pkg_failed=untried
+fi
+
+
+
+if test $pkg_failed = yes; then
+   	{ $as_echo "$as_me:${as_lineno-$LINENO}: result: no" >&5
+$as_echo "no" >&6; }
+
+if $PKG_CONFIG --atleast-pkgconfig-version 0.20; then
+        _pkg_short_errors_supported=yes
+else
+        _pkg_short_errors_supported=no
+fi
+        if test $_pkg_short_errors_supported = yes; then
+	        GSTREAMER_INTERFACES_PKG_ERRORS=`$PKG_CONFIG --short-errors --print-errors --cflags --libs "gstreamer-interfaces-0.10 >= $GSTREAMER_REQUIRED" 2>&1`
+        else
+	        GSTREAMER_INTERFACES_PKG_ERRORS=`$PKG_CONFIG --print-errors --cflags --libs "gstreamer-interfaces-0.10 >= $GSTREAMER_REQUIRED" 2>&1`
+        fi
+	# Put the nasty error message in config.log where it belongs
+	echo "$GSTREAMER_INTERFACES_PKG_ERRORS" >&5
+
+	as_fn_error $? "Package requirements (gstreamer-interfaces-0.10 >= $GSTREAMER_REQUIRED) were not met:
+
+$GSTREAMER_INTERFACES_PKG_ERRORS
+
+Consider adjusting the PKG_CONFIG_PATH environment variable if you
+installed software in a non-standard prefix.
+
+Alternatively, you may set the environment variables GSTREAMER_INTERFACES_CFLAGS
+and GSTREAMER_INTERFACES_LIBS to avoid the need to call pkg-config.
+See the pkg-config man page for more details." "$LINENO" 5
+elif test $pkg_failed = untried; then
+     	{ $as_echo "$as_me:${as_lineno-$LINENO}: result: no" >&5
+$as_echo "no" >&6; }
+	{ { $as_echo "$as_me:${as_lineno-$LINENO}: error: in \`$ac_pwd':" >&5
+$as_echo "$as_me: error: in \`$ac_pwd':" >&2;}
+as_fn_error $? "The pkg-config script could not be found or is too old.  Make sure it
+is in your PATH or set the PKG_CONFIG environment variable to the full
+path to pkg-config.
+
+Alternatively, you may set the environment variables GSTREAMER_INTERFACES_CFLAGS
+and GSTREAMER_INTERFACES_LIBS to avoid the need to call pkg-config.
+See the pkg-config man page for more details.
+
+To get pkg-config, see <http://pkg-config.freedesktop.org/>.
+See \`config.log' for more details" "$LINENO" 5; }
+else
+	GSTREAMER_INTERFACES_CFLAGS=$pkg_cv_GSTREAMER_INTERFACES_CFLAGS
+	GSTREAMER_INTERFACES_LIBS=$pkg_cv_GSTREAMER_INTERFACES_LIBS
+        { $as_echo "$as_me:${as_lineno-$LINENO}: result: yes" >&5
+$as_echo "yes" >&6; }
+
+fi
+
+pkg_failed=no
 { $as_echo "$as_me:${as_lineno-$LINENO}: checking for GST_PBU" >&5
 $as_echo_n "checking for GST_PBU... " >&6; }
 
@@ -14059,6 +14158,7 @@
                   gupnp-av-1.0
                   gupnp-dlna-1.0
                   gstreamer-0.10
+                  gstreamer-interfaces-0.10
                   gstreamer-pbutils-0.10
                   gio-2.0
                   gee-1.0
@@ -14087,6 +14187,7 @@
                   gupnp-av-1.0
                   gupnp-dlna-1.0
                   gstreamer-0.10
+                  gstreamer-interfaces-0.10
                   gstreamer-pbutils-0.10
                   gio-2.0
                   gee-1.0
diff -Naur rygel-0.14.3.old/configure.ac rygel-0.14.3/configure.ac
--- rygel-0.14.3.old/configure.ac	2012-08-07 21:39:00.000000000 +0200
+++ rygel-0.14.3/configure.ac	2012-10-14 20:33:49.055679879 +0200
@@ -53,6 +53,7 @@
 PKG_CHECK_MODULES(LIBGUPNP_DLNA, gupnp-dlna-1.0 >= $GUPNP_DLNA_REQUIRED)
 PKG_CHECK_MODULES(LIBGSTREAMER, gstreamer-0.10 >= $GSTREAMER_REQUIRED)
 PKG_CHECK_MODULES(GSTREAMER_BASE, gstreamer-base-0.10 >= $GSTREAMER_REQUIRED)
+PKG_CHECK_MODULES(GSTREAMER_INTERFACES, gstreamer-interfaces-0.10 >= $GSTREAMER_REQUIRED)
 PKG_CHECK_MODULES(GST_PBU, gstreamer-pbutils-0.10 >= $GSTPBU_REQUIRED)
 PKG_CHECK_MODULES(LIBGIO, gio-2.0 >= $GIO_REQUIRED)
 PKG_CHECK_MODULES(GEE, gee-1.0 >= $GEE_REQUIRED)
@@ -82,6 +83,7 @@
                   gupnp-av-1.0
                   gupnp-dlna-1.0
                   gstreamer-0.10
+                  gstreamer-interfaces-0.10
                   gstreamer-pbutils-0.10
                   gio-2.0
                   gee-1.0
diff -Naur rygel-0.14.3.old/src/plugins/playbin/Makefile.in rygel-0.14.3/src/plugins/playbin/Makefile.in
--- rygel-0.14.3.old/src/plugins/playbin/Makefile.in	2012-08-07 21:48:50.000000000 +0200
+++ rygel-0.14.3/src/plugins/playbin/Makefile.in	2012-10-14 20:38:42.751643056 +0200
@@ -16,6 +16,23 @@
 @SET_MAKE@
 
 VPATH = @srcdir@
+am__make_dryrun = \
+  { \
+    am__dry=no; \
+    case $$MAKEFLAGS in \
+      *\\[\ \	]*) \
+        echo 'am--echo: ; @echo "AM"  OK' | $(MAKE) -f - 2>/dev/null \
+          | grep '^AM OK$$' >/dev/null || am__dry=yes;; \
+      *) \
+        for am__flg in $$MAKEFLAGS; do \
+          case $$am__flg in \
+            *=*|--*) ;; \
+            *n*) am__dry=yes; break;; \
+          esac; \
+        done;; \
+    esac; \
+    test $$am__dry = yes; \
+  }
 pkgdatadir = $(datadir)/@PACKAGE@
 pkgincludedir = $(includedir)/@PACKAGE@
 pkglibdir = $(libdir)/@PACKAGE@
@@ -83,7 +100,7 @@
 am__DEPENDENCIES_2 = $(am__DEPENDENCIES_1) $(am__DEPENDENCIES_1) \
 	$(am__DEPENDENCIES_1) $(am__DEPENDENCIES_1) \
 	$(am__DEPENDENCIES_1) $(am__DEPENDENCIES_1) \
-	$(am__DEPENDENCIES_1)
+	$(am__DEPENDENCIES_1) $(am__DEPENDENCIES_1)
 librygel_playbin_la_DEPENDENCIES = $(am__DEPENDENCIES_2)
 am_librygel_playbin_la_OBJECTS = rygel-playbin-player.lo \
 	rygel-playbin-plugin.lo
@@ -130,6 +147,11 @@
 am__v_GEN_0 = @echo "  GEN   " $@;
 SOURCES = $(librygel_playbin_la_SOURCES)
 DIST_SOURCES = $(librygel_playbin_la_SOURCES)
+am__can_run_installinfo = \
+  case $$AM_UPDATE_INFO_DIR in \
+    n|no|NO) false;; \
+    *) (install-info --version) >/dev/null 2>&1;; \
+  esac
 ETAGS = etags
 CTAGS = ctags
 DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
@@ -179,6 +201,8 @@
 GSTREAMER_APP_LIBS = @GSTREAMER_APP_LIBS@
 GSTREAMER_BASE_CFLAGS = @GSTREAMER_BASE_CFLAGS@
 GSTREAMER_BASE_LIBS = @GSTREAMER_BASE_LIBS@
+GSTREAMER_INTERFACES_CFLAGS = @GSTREAMER_INTERFACES_CFLAGS@
+GSTREAMER_INTERFACES_LIBS = @GSTREAMER_INTERFACES_LIBS@
 GSTREAMER_TAG_CFLAGS = @GSTREAMER_TAG_CFLAGS@
 GSTREAMER_TAG_LIBS = @GSTREAMER_TAG_LIBS@
 GST_PBU_CFLAGS = @GST_PBU_CFLAGS@
@@ -340,9 +364,9 @@
 rygeldir = $(top_srcdir)/src/rygel
 MAINTAINERCLEANFILES = Makefile.in
 AM_CFLAGS = $(LIBGUPNP_CFLAGS) $(LIBGUPNP_AV_CFLAGS) \
-	$(LIBGSTREAMER_CFLAGS) $(GSTREAMER_BASE_CFLAGS) $(GEE_CFLAGS) \
-	$(UUID_CFLAGS) $(COVERAGE_CFLAGS) -I$(rygeldir) \
-	-DDATA_DIR='"$(shareddir)"' \
+	$(LIBGSTREAMER_CFLAGS) $(GSTREAMER_BASE_CFLAGS) \
+	$(GSTREAMER_INTERFACES_CFLAGS) $(GEE_CFLAGS) $(UUID_CFLAGS) \
+	$(COVERAGE_CFLAGS) -I$(rygeldir) -DDATA_DIR='"$(shareddir)"' \
 	-DSYS_CONFIG_DIR='"$(sysconfigdir)"' \
 	-DPLUGIN_DIR='"$(plugindir)"' -DDESKTOP_DIR='"$(desktopdir)"' \
 	-include config.h -DG_LOG_DOMAIN='"Playbin"'
@@ -351,6 +375,7 @@
 	$(LIBGUPNP_AV_LIBS) \
 	$(LIBGSTREAMER_LIBS) \
 	$(GSTREAMER_BASE_LIBS) \
+	$(GSTREAMER_INTERFACES_LIBS) \
 	$(GEE_LIBS) \
 	$(UUID_LIBS) \
 	$(COVERAGE_LIBS)
@@ -364,7 +389,7 @@
 RYGEL_COMMON_VALAFLAGS = \
 	--pkg rygel-build-config --pkg gupnp-1.0 \
 	--vapidir=$(rygeldir) \
-	--pkg gupnp-av-1.0 --pkg gee-1.0 \
+	--pkg gupnp-av-1.0 --pkg gee-1.0 --pkg gstreamer-interfaces-0.10 \
 	$(RYGEL_DEBUG_VALAFLAGS) \
 	$(COVERAGE_VALAFLAGS)
 
@@ -414,7 +439,6 @@
 $(am__aclocal_m4_deps):
 install-pluginLTLIBRARIES: $(plugin_LTLIBRARIES)
 	@$(NORMAL_INSTALL)
-	test -z "$(plugindir)" || $(MKDIR_P) "$(DESTDIR)$(plugindir)"
 	@list='$(plugin_LTLIBRARIES)'; test -n "$(plugindir)" || list=; \
 	list2=; for p in $$list; do \
 	  if test -f $$p; then \
@@ -422,6 +446,8 @@
 	  else :; fi; \
 	done; \
 	test -z "$$list2" || { \
+	  echo " $(MKDIR_P) '$(DESTDIR)$(plugindir)'"; \
+	  $(MKDIR_P) "$(DESTDIR)$(plugindir)" || exit 1; \
 	  echo " $(LIBTOOL) $(AM_LIBTOOLFLAGS) $(LIBTOOLFLAGS) --mode=install $(INSTALL) $(INSTALL_STRIP_FLAG) $$list2 '$(DESTDIR)$(plugindir)'"; \
 	  $(LIBTOOL) $(AM_LIBTOOLFLAGS) $(LIBTOOLFLAGS) --mode=install $(INSTALL) $(INSTALL_STRIP_FLAG) $$list2 "$(DESTDIR)$(plugindir)"; \
 	}
@@ -485,9 +511,9 @@
 	@if test -f $@; then :; else \
 	  $(MAKE) $(AM_MAKEFLAGS) $(srcdir)/librygel_playbin_la_vala.stamp; \
 	fi
-$(srcdir)/librygel_playbin_la_vala.stamp: $(librygel_playbin_la_SOURCES)
+$(srcdir)/librygel_playbin_la_vala.stamp: rygel-playbin-player.vala rygel-playbin-plugin.vala
 	$(AM_V_at)rm -f $@ && echo stamp > $@-t
-	$(AM_V_VALAC)$(am__cd) $(srcdir) && $(VALAC) $(librygel_playbin_la_VALAFLAGS) $(VALAFLAGS) -C $(librygel_playbin_la_SOURCES)
+	$(AM_V_VALAC)$(am__cd) $(srcdir) && $(VALAC) $(librygel_playbin_la_VALAFLAGS) $(VALAFLAGS) -C rygel-playbin-player.vala rygel-playbin-plugin.vala
 	$(AM_V_at)mv -f $@-t $@
 
 mostlyclean-libtool:
diff -Naur rygel-0.14.3.old/src/plugins/playbin/rygel-playbin-player.c rygel-0.14.3/src/plugins/playbin/rygel-playbin-player.c
--- rygel-0.14.3.old/src/plugins/playbin/rygel-playbin-player.c	2012-08-07 21:50:34.000000000 +0200
+++ rygel-0.14.3/src/plugins/playbin/rygel-playbin-player.c	2012-10-14 20:41:42.118399125 +0200
@@ -1,4 +1,4 @@
-/* rygel-playbin-player.c generated by valac 0.14.2, the Vala compiler
+/* rygel-playbin-player.c generated by valac 0.18.0, the Vala compiler
  * generated from rygel-playbin-player.vala, do not modify */
 
 /*
@@ -28,6 +28,9 @@
 #include <glib-object.h>
 #include <rygel.h>
 #include <gst/gst.h>
+#if GST_CHECK_VERSION(0,10,25)
+#include <gst/interfaces/streamvolume.h>
+#endif
 #include <stdlib.h>
 #include <string.h>
 
@@ -48,6 +51,7 @@
 #define _gst_structure_free0(var) ((var == NULL) ? NULL : (var = (gst_structure_free (var), NULL)))
 #define _gst_caps_unref0(var) ((var == NULL) ? NULL : (var = (gst_caps_unref (var), NULL)))
 #define _g_error_free0(var) ((var == NULL) ? NULL : (var = (g_error_free (var), NULL)))
+#define _vala_assert(expr, msg) if G_LIKELY (expr) ; else g_assertion_message_expr (G_LOG_DOMAIN, __FILE__, __LINE__, G_STRFUNC, msg);
 
 struct _RygelPlaybinPlayer {
 	GObject parent_instance;
@@ -93,8 +97,6 @@
 static inline GstCaps* _dynamic_get_caps0 (GstElement* obj);
 static inline gchar* _dynamic_get_uri1 (GstElement* obj);
 static inline void _dynamic_set_uri2 (GstElement* obj, const gchar* value);
-static inline gdouble _dynamic_get_volume3 (GstElement* obj);
-static inline void _dynamic_set_volume4 (GstElement* obj, gdouble value);
 static void rygel_playbin_player_finalize (GObject* obj);
 static void _vala_rygel_playbin_player_get_property (GObject * object, guint property_id, GValue * value, GParamSpec * pspec);
 static void _vala_rygel_playbin_player_set_property (GObject * object, guint property_id, const GValue * value, GParamSpec * pspec);
@@ -118,11 +120,13 @@
 	GstBus* bus;
 	self = (RygelPlaybinPlayer*) g_object_new (object_type, NULL);
 	_tmp0_ = gst_element_factory_make ("playbin2", NULL);
-	gst_object_ref_sink (_tmp0_);
+	if (_tmp0_ != NULL) {
+		gst_object_ref_sink (_tmp0_);
+	}
 	_gst_object_unref0 (self->priv->playbin);
 	self->priv->playbin = _tmp0_;
 	_tmp1_ = self->priv->playbin;
-	g_assert (_tmp1_ != NULL);
+	_vala_assert (_tmp1_ != NULL, "this.playbin != null");
 	_tmp2_ = self->priv->playbin;
 	_tmp3_ = gst_element_get_bus (_tmp2_);
 	bus = _tmp3_;
@@ -273,7 +277,7 @@
 	gboolean _tmp13_;
 	g_return_val_if_fail (self != NULL, FALSE);
 	_tmp0_ = self->priv->playbin;
-	_tmp1_ = gst_bin_get_by_name (GST_IS_BIN (_tmp0_) ? ((GstBin*) _tmp0_) : NULL, "typefind");
+	_tmp1_ = gst_bin_get_by_name (G_TYPE_CHECK_INSTANCE_TYPE (_tmp0_, GST_TYPE_BIN) ? ((GstBin*) _tmp0_) : NULL, "typefind");
 	_gst_object_unref0 (typefind);
 	typefind = _tmp1_;
 	_tmp2_ = typefind;
@@ -322,7 +326,7 @@
 			_tmp2_ = message;
 			_tmp3_ = _tmp2_->src;
 			_tmp4_ = self->priv->playbin;
-			if (_tmp3_ == GST_OBJECT (_tmp4_)) {
+			if (_tmp3_ == G_TYPE_CHECK_INSTANCE_CAST (_tmp4_, GST_TYPE_OBJECT, GstObject)) {
 				GstState old_state = 0;
 				GstState new_state = 0;
 				GstMessage* _tmp5_;
@@ -525,33 +529,19 @@
 }
 
 
-static inline gdouble _dynamic_get_volume3 (GstElement* obj) {
-	gdouble result;
-	g_object_get (obj, "volume", &result, NULL);
-	return result;
-}
-
-
 static gdouble rygel_playbin_player_real_get_volume (RygelMediaPlayer* base) {
 	gdouble result;
 	RygelPlaybinPlayer* self;
 	GstElement* _tmp0_;
-	gdouble _tmp1_;
-	gdouble _tmp2_;
+	gdouble _tmp1_ = 0.0;
 	self = (RygelPlaybinPlayer*) base;
 	_tmp0_ = self->priv->playbin;
-	_tmp1_ = _dynamic_get_volume3 (_tmp0_);
-	_tmp2_ = _tmp1_;
-	result = _tmp2_;
+	_tmp1_ = gst_stream_volume_get_volume (G_TYPE_CHECK_INSTANCE_TYPE (_tmp0_, GST_TYPE_STREAM_VOLUME) ? ((GstStreamVolume*) _tmp0_) : NULL, GST_STREAM_VOLUME_FORMAT_CUBIC);
+	result = _tmp1_;
 	return result;
 }
 
 
-static inline void _dynamic_set_volume4 (GstElement* obj, gdouble value) {
-	g_object_set (obj, "volume", value, NULL);
-}
-
-
 static void rygel_playbin_player_real_set_volume (RygelMediaPlayer* base, gdouble value) {
 	RygelPlaybinPlayer* self;
 	GstElement* _tmp0_;
@@ -560,7 +550,7 @@
 	self = (RygelPlaybinPlayer*) base;
 	_tmp0_ = self->priv->playbin;
 	_tmp1_ = value;
-	_dynamic_set_volume4 (_tmp0_, _tmp1_);
+	gst_stream_volume_set_volume (G_TYPE_CHECK_INSTANCE_TYPE (_tmp0_, GST_TYPE_STREAM_VOLUME) ? ((GstStreamVolume*) _tmp0_) : NULL, GST_STREAM_VOLUME_FORMAT_CUBIC, _tmp1_);
 	_tmp2_ = value;
 	g_debug ("rygel-playbin-player.vala:114: volume set to %f.", _tmp2_);
 	g_object_notify ((GObject *) self, "volume");
@@ -657,7 +647,7 @@
 
 static void rygel_playbin_player_finalize (GObject* obj) {
 	RygelPlaybinPlayer * self;
-	self = RYGEL_PLAYBIN_PLAYER (obj);
+	self = G_TYPE_CHECK_INSTANCE_CAST (obj, RYGEL_PLAYBIN_TYPE_PLAYER, RygelPlaybinPlayer);
 	_gst_object_unref0 (self->priv->playbin);
 	_g_free0 (self->priv->_playback_state);
 	G_OBJECT_CLASS (rygel_playbin_player_parent_class)->finalize (obj);
@@ -680,7 +670,7 @@
 
 static void _vala_rygel_playbin_player_get_property (GObject * object, guint property_id, GValue * value, GParamSpec * pspec) {
 	RygelPlaybinPlayer * self;
-	self = RYGEL_PLAYBIN_PLAYER (object);
+	self = G_TYPE_CHECK_INSTANCE_CAST (object, RYGEL_PLAYBIN_TYPE_PLAYER, RygelPlaybinPlayer);
 	switch (property_id) {
 		case RYGEL_PLAYBIN_PLAYER_PLAYBACK_STATE:
 		g_value_take_string (value, rygel_media_player_get_playback_state ((RygelMediaPlayer*) self));
@@ -706,7 +696,7 @@
 
 static void _vala_rygel_playbin_player_set_property (GObject * object, guint property_id, const GValue * value, GParamSpec * pspec) {
 	RygelPlaybinPlayer * self;
-	self = RYGEL_PLAYBIN_PLAYER (object);
+	self = G_TYPE_CHECK_INSTANCE_CAST (object, RYGEL_PLAYBIN_TYPE_PLAYER, RygelPlaybinPlayer);
 	switch (property_id) {
 		case RYGEL_PLAYBIN_PLAYER_PLAYBACK_STATE:
 		rygel_media_player_set_playback_state ((RygelMediaPlayer*) self, g_value_get_string (value));
diff -Naur rygel-0.14.3.old/src/plugins/playbin/rygel-playbin-player.vala rygel-0.14.3/src/plugins/playbin/rygel-playbin-player.vala
--- rygel-0.14.3.old/src/plugins/playbin/rygel-playbin-player.vala	2012-08-07 21:39:00.000000000 +0200
+++ rygel-0.14.3/src/plugins/playbin/rygel-playbin-player.vala	2012-10-14 20:01:36.667081257 +0200
@@ -106,11 +106,11 @@
 
     public double volume {
         get {
-            return this.playbin.volume;
+            return (this.playbin as StreamVolume).get_volume (StreamVolumeFormat.CUBIC);
         }
 
         set {
-            this.playbin.volume = value;
+            (this.playbin as StreamVolume).set_volume (StreamVolumeFormat.CUBIC, value);
             debug ("volume set to %f.", value);
         }
     }
